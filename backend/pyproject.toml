[project]
name = "fog-analysis"
version = "0.1.0"
description = "Data cleaning, detector evaluation and firmware reference for the freezing-of-gait detector"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.141.1",
    "joblib",
    "numpy",
    "pandas",
    "scikit-learn",
    "scipy",
    "uvicorn>=0.53.0",
    "websockets>=17.1",
]
